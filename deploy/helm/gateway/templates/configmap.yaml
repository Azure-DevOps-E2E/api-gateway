apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexuscart.fullname" . }}-config
  labels:
    {{- include "nexuscart.labels" . | nindent 4 }}
data:
  nginx.conf: |
{{ tpl (.Files.Get "files/nginx.conf") . | nindent 4 }}
  health.html: |
{{ .Files.Get "files/health.html" | nindent 4 }}

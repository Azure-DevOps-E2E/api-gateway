{
  "name": "api-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "HTTP API gateway for the sample microservices system",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "check": "node --check src/gateway.js && node --check src/server.js && node --check test/gateway.test.js",
    "build": "node --check src/gateway.js && node --check src/server.js"
  }
}
